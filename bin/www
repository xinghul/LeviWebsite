#!/usr/bin/env node
var debug = require('debug')('Levi Lu');
var app = require('../server');

app.set('port', process.env.PORT ||  3001);

var server = app.listen(app.get('port'), function() {
    console.log("Levi Lu'server listening on port " + server.address().port);
});
